import styled from 'styled-components';

const Container = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(11.4286rem, 1fr));
  grid-gap: 2.8571rem;
`;

export { Container };
